{
  "manifest_version": 2,

  "name": "Getting started example",
  "description": "This extension allows the user to highlight elements on the current page.",
  "version": "1.0",

  "browser_action": {
    "default_icon": "devil-laugh.png",
    "default_popup": "popup.html"
  },
  "permissions": [
    "activeTab"
  ]
}